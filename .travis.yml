language: node_js

node_js:
  - '8'

sudo: enabled

notifications:
  email: false

before_install:
  - npm update

install:
  - npm install

script:
  - npm test

cache:
  directories:
    - node_modules
